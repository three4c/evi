import type { Keymap } from "@/utils";

export const VISUAL_KEYMAPS: Keymap = {
  expand_left: "h",
  expand_down: "j",
  expand_up: "k",
  expand_right: "l",
  replace: "p",
  copy: "y",
  cut: "x",
};
